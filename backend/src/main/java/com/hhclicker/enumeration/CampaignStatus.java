package com.hhclicker.enumeration;

public enum CampaignStatus {
    DRAFT,
    PREVIEW_LOADING,
    PREVIEW_READY,
    LETTERS_GENERATING,
    READY_TO_APPLY,
    APPLYING,
    STOPPING,
    STOPPED,
    COMPLETED,
    FAILED,
    INTERRUPTED
}
