package com.hhclicker.enumeration;

public enum ApplicationStatus {
    PENDING,
    SENDING,
    SENT,
    ALREADY_APPLIED,
    TEST_REQUIRED,
    LIMIT_EXCEEDED,
    AUTH_ERROR,
    SKIPPED,
    LETTER_GENERATION_FAILED,
    FAILED
}
